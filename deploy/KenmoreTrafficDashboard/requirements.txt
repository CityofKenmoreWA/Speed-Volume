# Pinned to the versions this build was tested against. Update deliberately,
# not incidentally: rebuild the venv, re-test, then bump these.
pandas==3.0.3
numpy==2.5.0
matplotlib==3.11.0
jinja2==3.1.6
streamlit==1.58.0
openpyxl==3.1.5          # reads the legacy *_Report.xlsx (speed-limit resolution)
XlsxWriter==3.2.9        # writes the Excel export
reportlab==5.0.0         # writes the PDF export
